# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-s3nICXuRNA
# Backend routes through Open WebUI
OPENAI_API_BASE_URL=http://open-webui:8080/api/v1

# MinIO Configuration (Object Storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=false

# Qdrant Configuration (Vector Database)
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# Data Directory (for ingestion)
# Path to your dataset directory containing PDFs, documents.json, and messages.json
DATA_DIR=/Users/etienneduplessix/Downloads/full_dataset_v2

# Backend Configuration
PORT=8000
DEBUG_RAG=true

# PostgreSQL Database Configuration
POSTGRES_DB=skoda_db
POSTGRES_USER=skoda_user
POSTGRES_PASSWORD=skoda_password

# Open WebUI Configuration
WEBUI_SECRET_KEY=eyJhbGciOiJI
USER_ROLE=HR
# Open WebUI API Key (for backend authentication)
# Get this from Open WebUI Settings -> API Key section
OPEN_WEBUI_API_KEY=sk-f1

# Local Embeddings Configuration
# Set to 'true' to use local open-source embeddings instead of OpenAI
USE_LOCAL_EMBEDDINGS=true
# Local embedding model (options: all-mpnet-base-v2, all-MiniLM-L6-v2, etc.)
LOCAL_EMBEDDING_MODEL=all-mpnet-base-v2
